---
export const prerender = false;

const { slug } = Astro.params;

const res = await fetch(
  `${import.meta.env.VITE_API_BASE_URL}/api/posts/${slug}?preview=true`
);

const post = await res.json();
---
<h1>Preview: {post.title}</h1>
