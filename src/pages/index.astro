---
import Layout from '../layouts/Layout.astro';

const API_BASE = import.meta.env.VITE_API_BASE_URL ?? 'http://localhost:3000';
console.log('SSR fetch using API_BASE:', API_BASE);

let posts: any[] = [];
let topPosts: any[] = [];
let adminPicks: any[] = [];
let categories: any[] = [];

try {
  const [postsRes, topRes, adminRes, catsRes] = await Promise.all([
    fetch(`${API_BASE}/api/posts?limit=6`).catch(() => null),
    fetch(`${API_BASE}/api/posts/top?limit=4`).catch(() => null),
    fetch(`${API_BASE}/api/posts/admin-picks?limit=4`).catch(() => null),
    fetch(`${API_BASE}/api/categories`).catch(() => null)
  ]);
  
  if (postsRes?.ok) {
    const postsData = await postsRes.json();
    posts = postsData.posts || [];
  }
  
  if (topRes?.ok) {
    const topData = await topRes.json();
    topPosts = topData.posts || [];
  }
  
  if (adminRes?.ok) {
    const adminData = await adminRes.json();
    adminPicks = adminData.posts || [];
  }
  
  if (catsRes?.ok) {
    categories = await catsRes.json() || [];
  }
} catch (err) {
  console.error('Failed to load data:', err);
}

function formatExcerpt(content: any): string {
  if (!content) return '';
  const text = String(content)
    .replace(/!\[.*?\]\(.*?\)/g, '')
    .replace(/[#*`]/g, '')
    .replace(/\n+/g, ' ')
    .trim();
  return text.length > 0 ? text.substring(0, 150) + '...' : '';
}

function extractImage(content: any): string | null {
  if (!content) return null;
  const match = String(content).match(/!\[.*?\]\((.*?)\)/);
  return match ? match[1] : null;
}
---

<Layout title="DevNewsletter - Stay Updated with Developer News" showHero={true}>

  <!-- Hero (SEO: H1 + expanded content) -->
  <section class="hero-section relative overflow-hidden py-20 md:py-32 bg-[var(--bg-body)]">
    <!-- Minimal geometric pattern background -->
      <div class="absolute inset-0 opacity-[0.03] dark:opacity-[0.02] bg-[var(--bg-body)]">
      <svg class="w-full h-full" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <pattern id="grid-pattern" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 40 0 L 0 0 0 40" fill="none" stroke="currentColor" stroke-width="1"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid-pattern)" class="text-gray-400 dark:text-zinc-700"/>
      </svg>
    </div>
    
    <!-- Subtle accent dots -->
    <div class="absolute top-20 right-20 w-2 h-2 bg-amber-500 rounded-full opacity-20"></div>
    <div class="absolute bottom-32 left-16 w-1.5 h-1.5 bg-orange-500 rounded-full opacity-20"></div>
    <div class="absolute top-1/2 right-1/4 w-1 h-1 bg-yellow-500 rounded-full opacity-15"></div>
    
    <div class="container relative z-10 text-center">
      <div class="inline-flex items-center gap-2 px-4 py-2 mb-8 bg-white/80 dark:bg-zinc-900/80 backdrop-blur-sm rounded-full border border-amber-200 dark:border-amber-800/50 shadow-lg">
        <span class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
        <span class="text-xs font-bold text-gray-700 dark:text-gray-300 uppercase tracking-widest">Fresh Content Weekly</span>
      </div>
      
      <h1 class="text-5xl md:text-7xl lg:text-8xl font-serif font-black leading-tight mb-8 text-gray-900 dark:text-white animate-fade-in-up">
        DevNewsletter
      </h1>
      <p class="text-2xl md:text-3xl font-serif font-bold text-gray-800 dark:text-gray-200 mb-4 animate-fade-in-up animation-delay-200">
        Concise developer news, tips, and practical tutorials
      </p>

      <div class="mx-auto max-w-3xl text-left prose mt-12">
        <p class="text-lg text-gray-700 dark:text-gray-300">
          DevNewsletter delivers short, actionable developer news and practical tutorials every week. Our goal is to help engineers and makers stay current without drowning in noise: you get concise explanations, tools and libraries worth trying, debugging notes from real projects, and short post-mortems that highlight what worked (and what didn't). Whether you're shipping products, experimenting with new stacks, or leading a small team, these notes are written so you can read them between tasks and actually apply them.
        </p>

        <p class="text-lg text-gray-700 dark:text-gray-300">
          Each issue includes curated links, a short tutorial or code snippet, and a few mental-model style reflections designed to improve your day-to-day engineering decisions. We emphasize clarity and usefulness: clear examples, minimal jargon, and links to dive deeper when you want. Subscribe to receive the weekly digest in your inbox and browse past issues for quick reference.
        </p>

        <p class="text-lg text-gray-700 dark:text-gray-300">
          Highlights: performance tips, accessibility reminders, small refactors that pay off, CLI tricks, and short post-mortems from real shipping stories. We keep the reading time under five minutes per issue so it actually fits into a busy schedule.
        </p>

        <div class="mt-8 flex flex-col sm:flex-row items-center gap-4">
          <a href="/newsletters" class="group px-8 py-4 bg-amber-500 hover:bg-amber-600 text-white rounded-2xl font-bold text-lg shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-300 flex items-center gap-2">
            Subscribe — Free
            <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
            </svg>
          </a>
          <div class="text-sm text-gray-600 dark:text-gray-400 font-medium">✨ No spam • Unsubscribe anytime</div>
        </div>
      </div>

      <!-- Social share (lightweight links) -->
      <div class="mt-10 flex items-center justify-center gap-4">
        <a id="share-twitter" class="group px-6 py-3 rounded-xl bg-sky-500 hover:bg-sky-600 text-white text-sm font-bold shadow-md hover:shadow-lg transform hover:scale-105 transition-all duration-300 flex items-center gap-2" href="#" onclick="window.open('https://twitter.com/intent/tweet?text=' + encodeURIComponent(document.title) + '&url=' + encodeURIComponent(window.location.href),'_blank','width=600,height=400'); return false;">
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
          Share on X
        </a>
        <a id="share-linkedin" class="group px-6 py-3 rounded-xl bg-blue-700 hover:bg-blue-800 text-white text-sm font-bold shadow-md hover:shadow-lg transform hover:scale-105 transition-all duration-300 flex items-center gap-2" href="#" onclick="window.open('https://www.linkedin.com/sharing/share-offsite/?url=' + encodeURIComponent(window.location.href),'_blank','width=800,height=600'); return false;">
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M4.98 3.5C4.98 4.88 3.87 6 2.5 6S0 4.88 0 3.5 1.11 1 2.48 1 4.98 2.12 4.98 3.5zM0 8h5v16H0zM8 8h4.8v2.2h.1c.7-1.3 2.4-2.2 4-2.2 4.3 0 5.1 2.8 5.1 6.4V24h-5V15.2c0-2.1 0-4.8-2.9-4.8-2.9 0-3.3 2.2-3.3 4.6V24H8z"/></svg>
          Share on LinkedIn
        </a>
      </div>
    </div>
  </section>

  <!-- Top Stories Section -->
  {topPosts.length > 0 && (
    <section class="top-stories-section relative py-20 bg-[var(--bg-body)] overflow-hidden">
      <!-- Subtle diagonal lines pattern -->
        <div class="absolute inset-0 opacity-[0.02] dark:opacity-[0.01] bg-[var(--bg-body)]">
        <svg class="w-full h-full" xmlns="http://www.w3.org/2000/svg">
          <defs>
            <pattern id="diagonal-lines" x="0" y="0" width="60" height="60" patternUnits="userSpaceOnUse">
              <line x1="0" y1="0" x2="60" y2="60" stroke="currentColor" stroke-width="1"/>
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#diagonal-lines)" class="text-gray-400 dark:text-zinc-700"/>
        </svg>
      </div>
      <div class="container relative z-10">
        <div class="flex flex-col sm:flex-row items-start sm:items-center gap-4 mb-12">
            <div class="w-16 h-16 flex items-center justify-center bg-amber-500 text-white rounded-2xl shadow-lg transform hover:rotate-12 transition-transform duration-300">
                <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 20 20"><path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path></svg>
            </div>
            <div>
                <h2 class="text-4xl md:text-5xl font-serif font-black leading-tight tracking-tight text-gray-900 dark:text-white">Top Stories</h2>
                <p class="text-gray-600 dark:text-zinc-400 text-sm font-bold uppercase tracking-[0.2em] mt-2">Most Read This Week</p>
            </div>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6">
          {topPosts.map((post: any) => {
            const img = extractImage(post.content) || '/og-image.jpg';
            const srcset = `${img}?w=480 480w, ${img}?w=800 800w, ${img}?w=1200 1200w`;
            return (
              <a href={`/posts/${post.id}`} class="group relative block aspect-[3/4] overflow-hidden rounded-2xl sm:rounded-3xl bg-gray-900 shadow-xl transition-all hover:-translate-y-2">
                <img 
                  src={img} 
                  srcset={srcset}
                  sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 25vw"
                  alt={post.title}
                  class="absolute inset-0 h-full w-full object-cover opacity-60 transition-transform duration-700 group-hover:scale-110 group-hover:rotate-2"
                  loading="lazy"
                  decoding="async"
                />
                <div class="absolute inset-0 bg-gradient-to-t from-black via-black/40 to-transparent"></div>
                <div class="absolute inset-0 p-4 sm:p-6 flex flex-col justify-end gap-2 sm:gap-3 text-white">
                  <div class="flex items-center gap-2 flex-wrap">
                      <span class="px-2 py-0.5 bg-amber-500 text-[10px] font-bold uppercase tracking-widest rounded-md">Trending</span>
                      <span class="text-[10px] text-gray-300 font-bold uppercase tracking-widest flex items-center gap-1">
                          <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/></svg>
                          {post.views}
                      </span>
                  </div>
                  <h3 class="text-lg sm:text-xl font-serif font-bold group-hover:text-amber-400 transition-colors leading-snug">{post.title}</h3>
                </div>
              </a>
            )
          })}
        </div>
    </section>
  )}

  <!-- Categories -->
  {categories.length > 0 && (
    <section class="categories-section relative overflow-hidden bg-[var(--bg-body)]">
      <!-- Minimal dot pattern -->
        <div class="absolute inset-0 opacity-[0.02] dark:opacity-[0.01] bg-[var(--bg-body)]">
        <svg class="w-full h-full" xmlns="http://www.w3.org/2000/svg">
          <defs>
            <pattern id="dot-pattern" x="0" y="0" width="20" height="20" patternUnits="userSpaceOnUse">
              <circle cx="10" cy="10" r="1" fill="currentColor"/>
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#dot-pattern)" class="text-gray-400 dark:text-zinc-700"/>
        </svg>
      </div>
      <div class="container relative z-10">
        <div class="text-center mb-12">
          <h2 class="text-4xl md:text-5xl font-serif font-black leading-tight mb-4 text-gray-900 dark:text-white">Browse by Category</h2>
          <p class="text-gray-600 dark:text-zinc-400 text-sm font-bold uppercase tracking-[0.2em]">Explore Topics</p>
        </div>
        <div class="categories-grid">
          {categories.map((cat: any, index: number) => {
            const colors = [
              'bg-amber-500',
              'bg-blue-500',
              'bg-purple-500',
              'bg-green-500',
              'bg-red-500',
              'bg-indigo-500'
            ];
            const colorClass = colors[index % colors.length];
            return (
              <a href={`/categories/${cat.slug}`} class="category-card group relative overflow-hidden">
                <div class="relative z-10 flex flex-col items-center gap-3">
                  <div class={`w-16 h-16 flex items-center justify-center ${colorClass} rounded-2xl shadow-md group-hover:shadow-lg group-hover:scale-110 transition-all duration-300`}>
                    <span class="category-icon text-3xl group-hover:scale-125 transition-transform duration-300">📁</span>
                  </div>
                  <span class="category-name font-bold group-hover:text-amber-600 dark:group-hover:text-amber-400 transition-colors">{cat.name}</span>
                </div>
              </a>
            )
          })}
        </div>
    </section>
  )}

  <!-- Admin Picks Section -->
  {adminPicks.length > 0 && (
    <section class="admin-picks-section relative py-20 border-b border-gray-100 dark:border-zinc-800/50 overflow-hidden bg-[var(--bg-body)]">
        <div class="container relative z-10">
        <div class="max-w-xl mx-auto mb-16 space-y-4 text-center">
            <div class="inline-flex items-center justify-center w-20 h-20 mx-auto mb-6 bg-amber-500 rounded-3xl shadow-lg">
            <svg class="w-10 h-10 text-white" fill="currentColor" viewBox="0 0 20 20"><path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path></svg>
            </div>
            <h2 class="text-4xl md:text-5xl font-serif font-black leading-tight text-gray-900 dark:text-white">Admin Picks</h2>
            <p class="text-gray-600 dark:text-zinc-400 font-serif italic text-lg leading-relaxed">Hand-curated insights and stories you might have missed.</p>
            <div class="h-1.5 w-24 bg-amber-500 mx-auto rounded-full"></div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 sm:gap-8 max-w-5xl mx-auto">
          {adminPicks.map((post: any) => {
            const img = extractImage(post.content) || '/og-image.jpg';
            const srcset = `${img}?w=320 320w, ${img}?w=640 640w`;
            return (
              <article class="group relative flex flex-col sm:flex-row gap-4 sm:gap-6 p-4 sm:p-6 bg-white dark:bg-zinc-900/70 rounded-2xl sm:rounded-3xl border-2 border-gray-100 dark:border-zinc-800/50 hover:border-amber-500/50 dark:hover:border-amber-500/30 hover:shadow-2xl transition-all duration-300 text-left overflow-hidden">
                <div class="shrink-0 w-full sm:w-40 h-40 sm:h-32 overflow-hidden rounded-xl sm:rounded-2xl border-2 border-gray-200 dark:border-zinc-700 group-hover:border-amber-500/50 transition-colors">
                  <img src={img} srcset={srcset} sizes="(max-width: 640px) 100vw, 160px" alt={post.title} class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" loading="lazy" decoding="async" />
                </div>
                <div class="flex flex-col justify-center gap-2 sm:gap-3 relative z-10">
                  <span class="inline-flex items-center gap-2 px-2 sm:px-3 py-1 bg-amber-500 text-white text-[10px] font-bold uppercase tracking-[0.2em] rounded-full w-fit">⭐ Editor's Choice</span>
                  <h3 class="text-lg sm:text-xl font-serif font-bold leading-tight group-hover:text-amber-600 dark:group-hover:text-amber-400 transition-colors">
                    <a href={`/posts/${post.id}`}>{post.title}</a>
                  </h3>
                  <a href={`/posts/${post.id}`} class="text-xs font-bold uppercase tracking-widest text-gray-400 dark:text-zinc-500 group-hover:text-amber-600 dark:group-hover:text-amber-400 transition-colors flex items-center gap-1">
                    Read Post <span class="group-hover:translate-x-1 transition-transform">&rarr;</span>
                  </a>
                </div>
              </article>
            )
          })}
        </div>
        </div>
    </section>
  )}

  <!-- Latest Posts -->
  <section class="posts-section relative overflow-hidden bg-[var(--bg-body)]" id="latest">
    <div class="container relative z-10">
      <div class="section-header flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-16">
        <div>
            <h2 class="text-4xl md:text-5xl font-serif font-black leading-tight text-gray-900 dark:text-white">Latest Stories</h2>
            <p class="text-gray-600 dark:text-zinc-400 text-sm font-bold uppercase tracking-[0.2em] mt-2">The Freshly Brewed Notes</p>
        </div>
          <a href="/posts" class="view-all group flex items-center gap-3 px-8 py-3 bg-gray-900 dark:bg-white text-white dark:text-zinc-900 rounded-2xl font-black text-xs tracking-widest uppercase hover:scale-105 transition-all shadow-lg hover:shadow-xl">
          <span>Explore All</span>
          <span class="group-hover:translate-x-1 transition-transform">&rarr;</span>
        </a>
      </div>
      
      {posts.length === 0 ? (
        <div class="empty-state">
          <p>No posts yet. Check back soon!</p>
        </div>
      ) : (
        <div class="posts-grid grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 sm:gap-8">
          {posts.map((post: any) => {
            return (
            <article class="post-card group">
              <div class="post-card-content p-4 sm:p-6 h-full flex flex-col">
                {extractImage(post.content) && (
                  <div class="post-image-container relative mb-6 sm:mb-8 aspect-[16/10] overflow-hidden rounded-xl sm:rounded-2xl border border-gray-100 dark:border-zinc-800">
                    <img src={extractImage(post.content)} srcset={`${extractImage(post.content)}?w=480 480w, ${extractImage(post.content)}?w=800 800w, ${extractImage(post.content)}?w=1200 1200w`} sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw" alt={post.title} class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105" loading="lazy" decoding="async" />
                    <div class="absolute inset-0 bg-gradient-to-t from-gray-900/40 to-transparent"></div>
                  </div>
                )}
                
                <div class="flex items-center gap-2 sm:gap-3 mb-4 sm:mb-5 flex-wrap">
                    <time class="text-[10px] font-bold text-gray-400 dark:text-zinc-500 uppercase tracking-widest">
                    {new Date(post.created_at).toLocaleDateString('en-US', {
                        month: 'short',
                        day: 'numeric',
                        year: 'numeric'
                    })}
                    </time>
                    <span class="w-1 h-1 bg-amber-500/30 rounded-full"></span>
                    <span class="text-[10px] font-bold text-amber-600 dark:text-amber-500 uppercase tracking-widest">Fresh Drop</span>
                </div>

                <h3 class="post-title text-xl sm:text-2xl font-serif font-bold mb-3 sm:mb-4 leading-tight group-hover:text-amber-600 dark:group-hover:text-amber-500 transition-colors">
                  <a href={`/posts/${post.id}`}>{post.title}</a>
                </h3>

                <p class="post-excerpt text-gray-600 dark:text-zinc-400 font-serif leading-relaxed mb-6 sm:mb-10 line-clamp-3 text-sm sm:text-base">
                  {formatExcerpt(post.content)}
                </p>

                <div class="mt-auto pt-4 sm:pt-6 border-t border-gray-100 dark:border-zinc-800/50">
                    <a href={`/posts/${post.id}`} class="read-more-btn inline-flex items-center gap-2 font-black text-[10px] text-gray-400 dark:text-zinc-500 tracking-widest uppercase hover:text-amber-600 dark:hover:text-amber-500 transition-all duration-300">
                        <span>Check it out</span>
                        <span class="text-amber-500">&rarr;</span>
                    </a>
                </div>
              </div>
            </article>
            )
          })}
        </div>
      )}
    </div>
  </section>
</Layout>

<style>
  .container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  /* Animations */

  /* Categories Section */
  .categories-section {
    padding: 6rem 0;
    border-bottom: 1px solid var(--color-gray-100);
  }

  .categories-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
    gap: 1.5rem;
  }

  .category-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    padding: 2rem 1.5rem;
    background: white;
    border-radius: 2rem;
    text-decoration: none;
    color: var(--text-main);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    border: 2px solid var(--color-gray-100);
    text-align: center;
    position: relative;
    min-height: 180px;
  }

  .category-card:hover {
    border-color: transparent;
    transform: translateY(-8px) scale(1.02);
    box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.15);
  }
  
  :global(.dark) .category-card {
    background: rgba(39, 39, 42, 0.8);
    border-color: rgba(63, 63, 70, 0.5);
  }
  
  :global(.dark) .category-card:hover {
    background: rgba(39, 39, 42, 0.95);
    box-shadow: 0 20px 40px -15px rgba(0, 0, 0, 0.8);
  }

  .category-name {
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 700;
  }

  /* Posts Section */
  .posts-section {
    padding: 8rem 0;
  }

  .post-card {
    background: white;
    border-radius: 2.5rem;
    overflow: hidden;
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
    border: 2px solid var(--color-gray-100);
    height: 100%;
  }
  
  :global(.dark) .post-card {
    background: rgba(24, 24, 27, 0.7);
    border-color: rgba(39, 39, 42, 0.5);
  }

  .post-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 30px 60px -20px rgba(0, 0, 0, 0.2);
    border-color: var(--color-primary);
  }
  
  :global(.dark) .post-card:hover {
    box-shadow: 0 30px 60px -20px rgba(0, 0, 0, 0.7);
    background: #18181b;
  }

  .empty-state {
    text-align: center;
    padding: 8rem 2rem;
    color: var(--color-gray-500);
    background: var(--color-gray-50);
    border-radius: 3rem;
    border: 2px dashed var(--color-gray-200);
  }

  @media (max-width: 1024px) {
    .hero-section h1 {
      font-size: clamp(2.5rem, 8vw, 4rem);
    }
  }

  @media (max-width: 768px) {
    .container {
        padding: 0 1.25rem;
    }
    
    .categories-grid {
      grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
      gap: 1rem;
    }

    .category-card {
      padding: 1.5rem 1rem;
      min-height: 160px;
    }

    .hero-section {
      padding: 2rem 0 3rem;
    }

    .hero-section h1 {
      font-size: clamp(2rem, 10vw, 3rem);
      margin-bottom: 1rem;
    }

    .posts-section {
      padding: 4rem 0;
    }
  }

  @media (max-width: 640px) {
    .categories-grid {
      grid-template-columns: 1fr 1fr;
    }

    .hero-section .prose {
      font-size: 0.95rem;
    }

    .hero-section .prose p {
      font-size: 1rem;
    }
  }
</style>
